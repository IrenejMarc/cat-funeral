<!DOCTYPE html>
<html>
<head>
  <title>Funeral</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <%= link_to 'HOME', reservations_path %>
  </header>

  <main>
    <%= yield %>
  </main>

  <footer>
    <% if current_user %>
      You are logged in as <em><%= current_user.username %></em> |
      <%= link_to 'Log out', logout_path %>
    <% else %>
      You are not logged in. |
      <%= link_to 'Log in', login_path %>
    <% end %>
  </footer>
</body>
</html>
